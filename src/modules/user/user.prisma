model User {
    id       String @id @default(uuid())
    email    String @unique
    password String
    name     String
    Post     Post[]
}

model Post {
    id     String @id @default(uuid())
    user   User   @relation(fields: [userId], references: [id])
    userId String
}
